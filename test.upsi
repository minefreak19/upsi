dim length; 
unit metre: length;  
dim time; 
unit second: time;

unit mile: length = 1.609344 * 1000 metre;

unit yard: length = ((1 mile) / 1760); 

let y: length;
let x: length = 1 mile; 
let z: length = 2 * x;

// This is a comment
y = z / 0.33;

print(y);
print(y in metre);

// print(z*x/y in metre);
// print(z*x/(y in metre));
